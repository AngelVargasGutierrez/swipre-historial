{
  "name": "swipre-historial",
  "version": "1.0.0",
  "private": true,
  "description": "Proyecto aislado para probar RF-003 (Visualizar historial de acciones) del sistema MOPGIMED/SIPGIMED, sin depender de la base de datos ni del backend completo.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "vitest": "^2.1.5",
    "@playwright/test": "^1.48.0"
  }
}
